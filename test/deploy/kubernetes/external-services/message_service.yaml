apiVersion: v1
kind: Service
metadata:
  name: cortx-message-service
  namespace: default 
spec:
  selector:
    app: cortx-message-server
  clusterIP: None
  ports:
    - name: port-28300
      port: 28300
      targetPort: 28300
      protocol: TCP
