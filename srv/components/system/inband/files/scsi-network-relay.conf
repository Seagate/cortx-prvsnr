{% set stream_id = grains['id'].split('-')[1] %}
# Default settings for the first host and array 
address = 10.0.0.1/24
mc-a-address = 10.0.0.2
mc-b-address = 10.0.0.3
fix-addresses = false
stream-id = {{ stream_id }}
