apiVersion: v1
kind: PersistentVolume
metadata:
  name: cortx-pv-shared
spec:
  storageClassName: shared-pv
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  hostPath:
    path: /data/pv/shared