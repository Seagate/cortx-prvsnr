apiVersion: v1
kind: Service
metadata:
  name: setup
  labels:
    app: cortx-provisioner
spec:
  clusterIP: None
  selector:
    app: cortx-provisioner
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

