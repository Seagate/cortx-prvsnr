apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cortx-pvc-shared
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: shared-pv
  volumeMode: Filesystem
  resources:
    requests:
      storage: 20Gi
  volumeName: cortx-pv-shared
