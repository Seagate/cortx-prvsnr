{% set node = grains['id'] %}
s3servers: [ {{ salt['pillar.get']('cluster:{0}:fqdn'.format(node), 'localhost') }} ]
clovis_apps: [ {{ salt['pillar.get']('cluster:{0}:fqdn'.format(node), 'localhost') }} ]
confds: [ {{ salt['pillar.get']('cluster:{0}:fqdn'.format(node), 'localhost') }} ]
ssus:
  - host: {{ salt['pillar.get']('cluster:{0}:fqdn'.format(node), 'localhost') }}
    disks: {{ pillar['cluster'][node]['storage']['data_devices'] }}
