apiVersion: v1
kind: PersistentVolume
metadata:
  name: cortx-pv-config
spec:
  storageClassName: config-pv
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  hostPath:
    path: /data/pv/config