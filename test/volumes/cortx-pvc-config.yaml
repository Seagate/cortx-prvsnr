apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cortx-pvc-config
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: config-pv
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  volumeName: cortx-pv-config