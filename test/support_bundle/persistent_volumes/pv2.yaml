apiVersion: v1
kind: PersistentVolume
metadata:
  name: sb-write-pv
  labels:
    type: local
spec:
  storageClassName: support
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/opt"
